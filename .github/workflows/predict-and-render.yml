name: predict-and-render

env:
  # rclone hang guard — every R2 transfer in this workflow inherits these.
  # A failed/stalled transfer retries hard and a dead connection times out
  # fast, instead of hanging silently to the job ceiling (a job timeout
  # surfaces as a `cancelled` run, easy to miss).
  RCLONE_TIMEOUT: 5m
  RCLONE_CONTIMEOUT: 1m
  RCLONE_LOW_LEVEL_RETRIES: "20"
  RCLONE_RETRIES: "5"

# Combined predict + site render, running every 6 hours paired with collect.
# The original `predict.yml` and `render-site.yml` workflows are kept for
# manual dispatch only and no longer auto-trigger anything — see those
# files for per-step semantics. The bodies below are merged with no
# behaviour changes other than:
#   - one shared build + R2 sync
#   - predictions pushed to R2 once between predict and render so the deployed
#     site reads its own freshly-written outputs
#
# Dispatch: chained off s3-collect's completion by the weatherblend-scheduler
# Cloudflare Worker (handleWorkflowRun, Hop D) — NOT cron'd. The raw S3
# exact-runtime cycles s3-collect lands feed the 2d/3d predictors below, so
# the render runs the moment s3-collect finishes (~03:1x, 09:1x, 15:1x,
# 21:1x UTC in practice). See cloudflare/scheduler-worker/ for the full
# schedule + chain rationale.
#
# Both collects finish before this runs, enforced two ways:
# (1) the chain — the Worker dispatches this only on s3-collect's completion
#     webhook, and s3-collect itself runs after collect;
# (2) shared `weatherblend-data` concurrency group with cancel-in-progress=false
#     — a hard backstop: if anything overruns, predict queues until the lock
#     frees. cancel-in-progress=false so a job mid-write to predictions is
#     never interrupted.
on:
  # Schedule moved to Cloudflare 2026-05-01 (cloudflare/scheduler-worker/);
  # GitHub's `schedule:` trigger removed then. This workflow is no longer
  # cron'd at all — the Worker chains it off s3-collect's workflow_run
  # completion (see that directory's README / wrangler.toml).
  #
  # `workflow_dispatch:` retained so manual dashboard runs and the
  # Cloudflare scheduler's API-driven dispatches still work.
  workflow_dispatch:
    inputs:
      for_date:
        description: "Retroactive predict fill (yyyy-MM-dd). Leave blank for live run."
        required: false
        type: string
      window_days:
        description: "Render: how many days of predictions to include (table + charts)."
        required: false
        default: "30"
        type: string
      rolling_window_days:
        description: "Render: rolling-MAE window size for verification charts."
        required: false
        default: "14"
        type: string

concurrency:
  group: weatherblend-data
  cancel-in-progress: false

jobs:
  predict-and-render:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: "10.0.x"
          dotnet-quality: preview

      # All R2 trees (incl. data/forecasts) come via the shared composite
      # action so render-site.yml and this workflow can never drift on the
      # dependency list. data/forecasts moved into the composite on
      # 2026-05-04 — render needs it for the per-NWP PoP overlay on
      # rain-skill and the Met Office Spot line on temp-skill.
      - uses: ./.github/actions/sync-render-inputs
        with:
          bucket: ${{ secrets.R2_BUCKET }}
          access_key_id: ${{ secrets.R2_ACCESS_KEY_ID }}
          secret_access_key: ${{ secrets.R2_SECRET_ACCESS_KEY }}
          endpoint: ${{ secrets.R2_ENDPOINT }}

      - name: Build
        run: dotnet build src/WeatherBlend/WeatherBlend.csproj -c Release --nologo

      # Every predict target in dependency order — temp / precip / dry-window /
      # elements / feels-like / start-hour. Soft-skip codes per target are
      # documented in the composite. Shared verbatim with predict.yml so the
      # two workflows can never drift on which targets run.
      - uses: ./.github/actions/predict-all
        with:
          for_date: ${{ github.event.inputs.for_date }}

      # Phase 4b synthesis — 2-way arithmetic mean of phase 4a (BART)
      # and phase 3e (MLP) P(wet). Runs INSIDE predict-and-render
      # (rather than its own cron) so the same cycle's fresh 3e
      # parquet (just written by predict-all) feeds the mean
      # alongside whatever 4a is most recent on R2. .NET command
      # implemented in Phase4bPredictCommand.cs — same DuckDB +
      # ParquetSerializer plumbing as the rest of the predict path.
      #
      # Exit code semantics (per-station soft-skip handled inside the
      # command):
      #   0 — at least one station wrote 4b rows.
      #   2 — no precipitation stations configured (config bug) — FATAL.
      #   3 — every station fell through (no 4b manifest entry AND no
      #       4a/3e match anywhere). Common at the start of the UTC
      #       day on manual triggers that land before WeatherProb's
      #       03:00/09:00/15:00/21:00 UTC 4a cron has fired for the
      #       current anchor; the natural 03:15/09:15/15:15/21:15 UTC
      #       predict-and-render ticks pick it up once 4a is done.
      #       Soft-skipped since 2026-05-15 — missing 4a inputs is a
      #       timing artefact, not a 4b failure. The original same-
      #       anchor join bug that motivated propagation was fixed by
      #       the 2026-05-13 back-look; if it ever regresses we'll
      #       see "wrote 0" across every cycle and can re-tighten.
      - name: Synthesise Phase 4b (2-way mean of 4a + 3e)
        run: |
          code=0
          dotnet run --project src/WeatherBlend --no-build -c Release -- phase4b-predict || code=$?
          if [ "$code" != "0" ] && [ "$code" != "3" ]; then exit "$code"; fi

      - name: Push predictions to R2
        # Pushed BEFORE render so the deployed site is reading the same state
        # any external consumer would see in R2. copy (not sync) — additive only.
        # Includes the freshly-synthesised 4b parquet from the step above.
        env:
          R2_BUCKET: ${{ secrets.R2_BUCKET }}
        run: |
          rclone copy ./data/predictions "r2:${R2_BUCKET}/data/predictions" --fast-list --transfers 16 --checkers 32 --progress

      - name: Render site
        run: |
          WINDOW="${{ github.event.inputs.window_days }}"
          ROLLING="${{ github.event.inputs.rolling_window_days }}"
          ARGS="--output data/site"
          if [ -n "$WINDOW" ]; then ARGS="$ARGS --window-days $WINDOW"; fi
          if [ -n "$ROLLING" ]; then ARGS="$ARGS --rolling-window-days $ROLLING"; fi
          dotnet run --project src/WeatherBlend --no-build -c Release -- render-site $ARGS

      - name: Deploy to Cloudflare Pages
        uses: cloudflare/wrangler-action@v3
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          # --branch main promotes to the production URL (weatherblend.pages.dev);
          # without it the deploy would only get a preview URL.
          command: pages deploy data/site --project-name weatherblend --branch main --commit-dirty=true

      - name: Upload logs as artifact
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: predict-and-render-logs-${{ github.run_id }}
          path: logs/
          retention-days: 5
          if-no-files-found: ignore
